{
  programs.nvf.settings.vim.utility = {
    images.image-nvim = {
      enable = true;
      setupOpts = {
        backend = "kitty";
      };
      #preview.markdownPreview.enable = true;
    };
  };
}
